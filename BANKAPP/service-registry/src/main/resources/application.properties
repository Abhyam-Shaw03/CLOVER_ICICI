spring.application.name=service-registry
server.port=8761

# Eureka Server Configuration
eureka.instance.hostname=localhost
eureka.client.fetch-registry=false
eureka.client.register-with-eureka=false

# Prevent Unnecessary Timeouts (Recommended)
eureka.server.wait-time-in-ms-when-sync-empty=0

# Improve Eureka Server Stability
eureka.server.enable-self-preservation=true
eureka.server.response-cache-update-interval-ms=3000

########### LOGGING ############

# Enable detailed startup logging
logging.level.org.springframework=INFO
logging.level.org.springframework.boot=INFO
logging.level.org.springframework.web=INFO
logging.level.root=INFO

# Log output format
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n

# Log file (optional)
logging.file.name=logs/service-registry.log

########### HEARTBEAT & EVICTION SETTINGS (OPTIONAL) ############
#eureka.instance.lease-renewal-interval-in-seconds=15   # Default: 30 sec
#eureka.instance.lease-expiration-duration-in-seconds=30  # Default: 90 sec
#eureka.server.eviction-interval-timer-in-ms=5000  # Default: 60000 (Evict every 5 sec)